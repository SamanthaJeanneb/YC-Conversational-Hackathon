# Voice worker dependencies — STT/LLM/TTS all run through LiveKit Inference,
# so NO provider plugins/extras are needed. Only local VAD + turn detector.
# Install with:  pip install -r requirements.txt
livekit-agents[silero,turn-detector]~=1.5
python-dotenv
